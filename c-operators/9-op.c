/*increment|decrement arithmetic operator|postfix type*/
// original value is returned first before increment or decremment

#include <stdio.h>

int main()

{
	int a = 5, b = 6;
	printf("A=%d\n", a--);
	printf("B=%d\n", b--);
	return 0;
}
